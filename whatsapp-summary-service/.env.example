# --- Pusher (used by api/lib/whatsapp-shared.js for incoming commands) ---
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=

# --- Twilio (inbound webhook + outbound when provider=twilio) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# --- WhatsApp Cloud API (Meta) - optional ---
WHATSAPP_VERIFY_TOKEN=    # Secret string for webhook GET verification (set same in Meta App Dashboard)
WHATSAPP_CLOUD_API_TOKEN= # Permanent access token
WHATSAPP_PHONE_NUMBER_ID= # Phone number ID from Meta

# --- Default outbound provider for send-notification, answer-question, summarize-and-deliver ---
# Use "twilio" or "meta". If unset, defaults to "twilio".
DEFAULT_WHATSAPP_PROVIDER=twilio

# --- OpenAI (transcription in Twilio webhook; summarization / Q&A) ---
OPENAI_API_KEY=

# --- SMTP (summarize-and-deliver email) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_SENDER=
